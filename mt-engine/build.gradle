dependencies {
    compile project(':mt-api')
    compileOnly "org.projectlombok:lombok:${lombokVersion}"

    integrationTestCompile sourceSets.main.output
    integrationTestCompile sourceSets.test.output
}